
    /*
    ___________
    <div>{ships.length > 0 && shipList()}</div>

    ___________
    <div>{ships.length <= 0 || shipList()}</div>

    ___________
    <div>{!ships.length <= 0 && shipList()}</div>
    */